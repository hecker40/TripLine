# Site-only credentials. Never set VITE_* secrets or commit .env.local.
# This does not read or change OpenClaw's OPENAI_API_KEY.
TRAVELOS_OPENAI_API_KEY=
TRAVELOS_OPENAI_MODEL=gpt-4o-mini
PORT=8787
HOST=127.0.0.1
